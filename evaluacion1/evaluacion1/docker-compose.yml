version: "3.8"
services:
  mysql-db:
    image: mysql
    restart: always
    volumes:
      - ./data/db:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: marcelovp
      MYSQL_DATABASE: milkstgo
    ports:
      - 33060:3306
  app:
    container_name: proyecto_docker
    image: marcelovargaspavez/evaluacion1tingeso
    ports:
      - "8090:8090"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql-db:3306/milkstgo?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=marcelovp
    deploy:
      restart_policy:
        condition: on-failure
    depends_on:
      - mysql-db